import cx from "classnames";
import styles from "./styles.module.scss";

export default function Footer() {
  return (
    <footer className="flex justify-between container p-3 max-w-7xl mx-auto">
      <span>&copy; HighScoreWinsMoney 2022</span>
      <span>Twitter</span>
    </footer>
  );
}
